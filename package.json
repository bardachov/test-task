{
  "name": "test-task",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "rm -rf ./build && tsc && cpy **/* !**/*.ts ../build --cwd=src --no-overwrite --parents",
    "dev": "cross-env NODE_ENV=development nodemon src/server.ts",
    "start": "cross-env NODE_ENV=production node build/server.js"
  },
  "license": "MIT",
  "dependencies": {
    "cross-env": "^7.0.3",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "mongoose": "^6.0.8",
    "sass": "^1.42.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/mongoose": "^5.11.97",
    "@types/node": "^16.10.1",
    "cpy-cli": "^3.1.1",
    "nodemon": "^2.0.13",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
